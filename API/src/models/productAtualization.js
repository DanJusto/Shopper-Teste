class ProductAtualization {
  
  constructor(code, name, currentPrice, newPrice, validation) {
    this.code = code;
    this.name = name;
    this.currentPrice = currentPrice;
    this.newPrice = newPrice;
    this.validation = validation;
  }
  
}

module.exports = ProductAtualization