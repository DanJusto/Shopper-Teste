import { BrowserRouter } from "react-router-dom";
import { Router } from "./router";

export function Routes() {

    return (
        <BrowserRouter>
          <Router/>
        </BrowserRouter>
    )
}